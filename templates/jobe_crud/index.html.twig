{% extends 'base1.html.twig' %}

{% block title %}Jobe index{% endblock %}

{% block body %}
    <h1>Jobe index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Username</th>
                <th>Email</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for jobe in jobes %}
            <tr>
                <td>{{ jobe.id }}</td>
                <td>{{ jobe.username }}</td>
                <td>{{ jobe.email }}</td>
                <td>
                    <a href="{{ path('app_jobe_c_r_u_d_show', {'id': jobe.id}) }}">show</a>
                    <a href="{{ path('app_jobe_c_r_u_d_edit', {'id': jobe.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_jobe_c_r_u_d_new') }}">Create new</a>
{% endblock %}
