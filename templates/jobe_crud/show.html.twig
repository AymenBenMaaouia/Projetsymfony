{% extends 'base.html.twig' %}

{% block title %}Jobe{% endblock %}

{% block body %}
    <h1>Jobe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ jobe.id }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ jobe.username }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ jobe.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_jobe_c_r_u_d_index') }}">back to list</a>

    <a href="{{ path('app_jobe_c_r_u_d_edit', {'id': jobe.id}) }}">edit</a>

    {{ include('jobe_crud/_delete_form.html.twig') }}
{% endblock %}
